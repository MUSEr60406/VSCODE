#include<bits/stdc++.h>
#define pii pair<int,int>
#define pll pair<long,long>
#define ll long long
using namespace std;
vector<int> par;
vector<int> path;
int main()
{
    ios::sync_with_stdio(0), cin.tie(0), cout.tie(0);
    int t;
    cin.ignore();
    cin >> t;
    while(t--)
    {
        int m, n;
        cin >> m >> n;
        par.assign(26, -1);
        path.assign(26, -1);
    }

    return 0;
}