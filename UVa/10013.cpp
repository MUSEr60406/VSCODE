#include<bits/stdc++.h>
#define pii pair<int,int>
#define pll pair<long,long>
#define ll long long
using namespace std;

int main()
{
    ios::sync_with_stdio(0), cin.tie(0), cout.tie(0);
    int t, m;
    cin >> t;
    while(t--)
    {
        cin >> m;
        string n1 = "", n2 = "";
        char a, b;
        for(int i = 0 ; i < m ; i++)
        {
            cin >> a >> b;
            n1 += a;
            n2 += b;
        }
        for(int i = m - 1 ; i >= 0 ; i--)
        {
            
        }
    }

    return 0;
}